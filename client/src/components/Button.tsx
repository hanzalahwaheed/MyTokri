
const Button = ({ content }: { content: string }) => {
  return <button className="p-4 my-4 bg-slate-200 text-zinc-400 w-full rounded text-3xl font-bold">{content}</button>;
};

export default Button;
